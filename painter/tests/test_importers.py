
"""
TODO: Write this.

Include tests for the base importer at least, using the Test Cards.xlsx file.
Look into whether it's possible to write Django template rendering tests without
installing some frontend integration test system.
Captured here: https://github.com/adam-thomas/imperial-painter/issues/47

As of writing this comment, the importers are known to work stably, so testing
might not happen until I need to make significant changes.

There were some model/view tests here previously; I've removed them for being
trivial, since they mostly checked built-in Django functionality. This lets me
trim out some unnecessary requirements as well.
"""
